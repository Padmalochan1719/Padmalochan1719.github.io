const words=['Java','Spring Boot','REST APIs','Oracle','Clean Architecture'];let wi=0,ci=0,deleting=false;const revealAll=()=>document.querySelectorAll('.reveal').forEach(e=>e.classList.add('revealed'));revealAll();const typing=document.getElementById('typingText');function typeLoop(){if(!typing)return;const word=words[wi];typing.textContent=deleting?word.slice(0,ci--):word.slice(0,ci++);if(!deleting&&ci>word.length){deleting=true;setTimeout(typeLoop,1000);return}if(deleting&&ci<0){deleting=false;wi=(wi+1)%words.length;ci=0}setTimeout(typeLoop,deleting?45:90)}typeLoop();const nav=document.getElementById('nav'),menu=document.getElementById('menuBtn');if(menu)menu.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('.nav-link').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const theme=document.getElementById('themeToggle');if(theme)theme.addEventListener('click',()=>{document.body.classList.toggle('light');const light=document.body.classList.contains('light');document.documentElement.style.setProperty('--bg',light?'#f4faf6':'#07110d');document.documentElement.style.setProperty('--card',light?'#fff':'#102018');document.documentElement.style.setProperty('--card-solid',light?'#fff':'#102018');document.documentElement.style.setProperty('--text',light?'#102018':'#f2f7f4');document.documentElement.style.setProperty('--muted',light?'#607068':'#9eafa6');theme.textContent=light?'☾':'☼'});const progress=document.getElementById('progressBar'),topButton=document.getElementById('topBtn');function onScroll(){const max=document.documentElement.scrollHeight-window.innerHeight;if(progress)progress.style.width=(max>0?window.scrollY/max*100:0)+'%';if(topButton)topButton.classList.toggle('show',window.scrollY>500);const sections=Array.from(document.querySelectorAll('section[id]'));let current='home';sections.forEach(s=>{if(window.scrollY>=s.offsetTop-180)current=s.id});document.querySelectorAll('.nav-link').forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+current))}window.addEventListener('scroll',onScroll,{passive:true});onScroll();if(topButton)topButton.addEventListener('click',()=>window.scrollTo({top:0,behavior:'smooth'}));const year=document.getElementById('year');if(year)year.textContent=new Date().getFullYear();